{
  "name": "armonik-community",
  "private": true,
  "scripts": {
    "prepare": "nuxi prepare",
    "dev": "nuxi dev",
    "build": "nuxi build",
    "generate": "nuxi generate",
    "preview": "nuxi preview",
    "lint": "eslint --cache .",
    "lint:fix": "eslint --cache --fix .",
    "lint:md": "markdownlint . && case-police -d armonik-case-dict.json ./**/*.md",
    "lint:md:fix": "markdownlint . --fix && case-police --fix -d armonik-case-dict.json ./**/*.md"
  },
  "dependencies": {
    "@aneoconsultingfr/armonik-docs-theme": "^0.6.13",
    "nuxt": "^3.13.2"
  },
  "devDependencies": {
    "@nuxt/eslint-config": "^0.6.0",
    "case-police": "^0.7.0",
    "eslint": "^8.57.1",
    "markdownlint-cli": "^0.42.0"
  }
}
